resources:
  jobs:
    dbxmetagen_build_vector_index:
      name: "dbxmetagen_build_vector_index"

      parameters:
        - name: catalog_name
          default: ${var.catalog_name}
        - name: schema_name
          default: ${var.schema_name}
        - name: endpoint_name
          default: "dbxmetagen-vs"
        - name: sweep_stale_docs
          default: "false"

      tasks:
        - task_key: build_vector_index
          environment_key: default
          notebook_task:
            notebook_path: ${var.dbxmetagen_notebooks}/build_vector_index
            base_parameters:
              catalog_name: "{{job.parameters.catalog_name}}"
              schema_name: "{{job.parameters.schema_name}}"
              endpoint_name: "{{job.parameters.endpoint_name}}"
              sweep_stale_docs: "{{job.parameters.sweep_stale_docs}}"

      environments:
        - environment_key: default
          spec:
            client: "1"
            dependencies:
              - ${var.dbxmetagen_wheel}
