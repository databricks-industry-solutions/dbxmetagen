# Parallel Metadata Generation: Comment first, then PI + Domain
#
# Runs comments first, then PI and domain in parallel (so domain/PI can use
# existing comments when include_existing_table_comment is true). Same set of
# tables; defaults to apply_ddl=false so tables are added to
# metadata_generation_log for downstream analysis only.

resources:
  jobs:
    metadata_parallel_modes_job:
      name: "${var.app_name}_parallel_modes_job"
      max_concurrent_runs: 5

      permissions:
        - service_principal_name: ${resources.apps.dbxmetagen_app.service_principal_client_id}
          level: CAN_MANAGE_RUN

      email_notifications:
        on_failure:
          - ${var.current_user}

      parameters:
        - name: table_names
          default: ${var.job_table_names}
        - name: catalog_name
          default: ${var.catalog_name}
        - name: schema_name
          default: ${var.schema_name}
        - name: apply_ddl
          default: "false"
        - name: current_user
          default: ${var.current_user}
        - name: permission_groups
          default: ${var.permission_groups}
        - name: permission_users
          default: ${var.permission_users}
        - name: run_id
          default: "{{job.run_id}}"
        - name: include_previously_failed_tables
          default: "false"
        - name: use_kb_comments
          default: "false"
        - name: ontology_bundle
          default: ""
        - name: domain_config_path
          default: ""
        - name: sample_size
          default: "5"
        - name: include_lineage
          default: "false"
        - name: model
          default: "databricks-claude-sonnet-4-6"
        - name: spacy_model_names
          default: ${var.spacy_model_names}

      tasks:
        # Comments run first; PI and domain depend on it so they can use comments
        - task_key: generate_comments
          max_retries: 1
          min_retry_interval_millis: 600000
          timeout_seconds: 86400
          job_cluster_key: metadata_cluster
          notebook_task:
            notebook_path: ../../notebooks/generate_metadata.py
            base_parameters:
              mode: "comment"
              table_names: "{{job.parameters.table_names}}"
              catalog_name: "{{job.parameters.catalog_name}}"
              schema_name: "{{job.parameters.schema_name}}"
              apply_ddl: "{{job.parameters.apply_ddl}}"
              current_user: "{{job.parameters.current_user}}"
              permission_groups: "{{job.parameters.permission_groups}}"
              permission_users: "{{job.parameters.permission_users}}"
              run_id: "{{job.parameters.run_id}}"
              cleanup_control_table: "false"
              include_previously_failed_tables: "{{job.parameters.include_previously_failed_tables}}"
              ontology_bundle: "{{job.parameters.ontology_bundle}}"
              domain_config_path: "{{job.parameters.domain_config_path}}"
              sample_size: "{{job.parameters.sample_size}}"
              include_lineage: "{{job.parameters.include_lineage}}"
              model: "{{job.parameters.model}}"
              spacy_model_names: "{{job.parameters.spacy_model_names}}"
          libraries:
            - whl: ../../dist/*.whl

        - task_key: generate_pi
          max_retries: 1
          min_retry_interval_millis: 600000
          timeout_seconds: 86400
          depends_on:
            - task_key: generate_comments
          job_cluster_key: metadata_cluster
          notebook_task:
            notebook_path: ../../notebooks/generate_metadata.py
            base_parameters:
              mode: "pi"
              table_names: "{{job.parameters.table_names}}"
              catalog_name: "{{job.parameters.catalog_name}}"
              schema_name: "{{job.parameters.schema_name}}"
              apply_ddl: "{{job.parameters.apply_ddl}}"
              current_user: "{{job.parameters.current_user}}"
              permission_groups: "{{job.parameters.permission_groups}}"
              permission_users: "{{job.parameters.permission_users}}"
              run_id: "{{job.parameters.run_id}}"
              cleanup_control_table: "false"
              include_previously_failed_tables: "{{job.parameters.include_previously_failed_tables}}"
              include_existing_table_comment: "true"
              use_kb_comments: "{{job.parameters.use_kb_comments}}"
              ontology_bundle: "{{job.parameters.ontology_bundle}}"
              domain_config_path: "{{job.parameters.domain_config_path}}"
              sample_size: "{{job.parameters.sample_size}}"
              include_lineage: "{{job.parameters.include_lineage}}"
              model: "{{job.parameters.model}}"
              spacy_model_names: "{{job.parameters.spacy_model_names}}"
          libraries:
            - whl: ../../dist/*.whl

        - task_key: generate_domain
          max_retries: 1
          min_retry_interval_millis: 600000
          timeout_seconds: 86400
          depends_on:
            - task_key: generate_comments
          job_cluster_key: metadata_cluster
          notebook_task:
            notebook_path: ../../notebooks/generate_metadata.py
            base_parameters:
              mode: "domain"
              table_names: "{{job.parameters.table_names}}"
              catalog_name: "{{job.parameters.catalog_name}}"
              schema_name: "{{job.parameters.schema_name}}"
              apply_ddl: "{{job.parameters.apply_ddl}}"
              current_user: "{{job.parameters.current_user}}"
              permission_groups: "{{job.parameters.permission_groups}}"
              permission_users: "{{job.parameters.permission_users}}"
              run_id: "{{job.parameters.run_id}}"
              cleanup_control_table: "false"
              include_previously_failed_tables: "{{job.parameters.include_previously_failed_tables}}"
              include_existing_table_comment: "true"
              use_kb_comments: "{{job.parameters.use_kb_comments}}"
              ontology_bundle: "{{job.parameters.ontology_bundle}}"
              domain_config_path: "{{job.parameters.domain_config_path}}"
              sample_size: "{{job.parameters.sample_size}}"
              include_lineage: "{{job.parameters.include_lineage}}"
              model: "{{job.parameters.model}}"
              spacy_model_names: "{{job.parameters.spacy_model_names}}"
          libraries:
            - whl: ../../dist/*.whl

      job_clusters:
        - job_cluster_key: metadata_cluster
          new_cluster:
            apply_policy_default_values: true
            policy_id: ${var.policy_id}
            spark_version: 17.3.x-cpu-ml-scala2.13
            node_type_id: ${var.node_type}
            data_security_mode: SINGLE_USER
            autoscale:
              min_workers: 2
              max_workers: 4
            custom_tags:
              project: dbxmetagen