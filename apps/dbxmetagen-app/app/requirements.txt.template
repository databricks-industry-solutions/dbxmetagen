fastapi>=0.115.0
uvicorn>=0.32.0
databricks-sdk==0.68.0
databricks-langchain>=0.2.0
langchain-core>=0.3.0
langgraph>=1.1.3,<2.0.0
langgraph-prebuilt>=1.0.8,<2.0.0
requests>=2.25.0
pydantic>=2.9.0
pyyaml>=6.0
cachetools>=5.3.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
openpyxl>=3.1.0
databricks-vectorsearch>=0.40,<0.74
sqlparse>=0.5.0
rdflib>=6.3.0
# Keep mcp UNPINNED past a low floor: the app's langchain stack (databricks-langchain
# / langchain-mcp-adapters) transitively constrains mcp to an OLDER release, so forcing
# mcp>=1.13 makes the app build fail with ResolutionImpossible. The code is written to
# work on the old mcp the stack resolves (annotation-safe -- no stringified annotations),
# verified on mcp 1.12.0. Do NOT raise this floor without re-checking the app build.
mcp>=1.9,<2
./__WHL_NAME__
