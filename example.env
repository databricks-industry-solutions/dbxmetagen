# Environment-specific configuration for deployment
# Copy this file to dev.env and fill in your values
# dev.env is gitignored and will not be committed

# Databricks workspace URL
DATABRICKS_HOST=<databricks_url>

# Deployment target (dev, staging, prod, etc.)
TARGET=dev

# Comma-separated list of groups to grant read access to created objects
# Example: "account users, data_engineers, analysts"
# Leave empty or omit to skip group grants
permission_groups=account users

# Comma-separated list of users to grant read access to created objects  
# Example: "user1@company.com, user2@company.com"
# Leave empty or omit to skip user grants
permission_users=<person@email.com>